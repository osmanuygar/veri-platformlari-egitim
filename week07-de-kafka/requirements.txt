# Hafta 7 — Apache Kafka
# Kurulum:  pip install -r requirements.txt

confluent-kafka==2.5.3      # Producer/Consumer + Schema Registry istemcisi
fastavro==1.9.7             # Avro serileştirme (AvroSerializer bunu kullanır)
requests==2.32.3            # Connect REST API çağrıları
psycopg2-binary==2.9.9      # CDC kaynağına yazmak için
Faker==30.3.0               # Sahte sipariş verisi
python-dotenv==1.0.1        # .env okuma
tabulate==0.9.0             # Lag tablosunu düzgün yazdırmak için
