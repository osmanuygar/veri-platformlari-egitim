# Hafta 6 — varsayılan ayarlar (eğitim ortamı, gizli bilgi içermez)
# Kullanım:  cp .env.example .env

PGHOST=localhost
PGPORT=5436
PGUSER=de_user
PGPASSWORD=de_pass
PGDATABASE=de_db

AIRFLOW_URL=http://localhost:8090
AIRFLOW_USER=admin
AIRFLOW_PASSWORD=admin

# dbt profiles.yml bu değişkenleri kullanır (bkz. dbt_project/profiles/profiles.yml)
DBT_PROFILES_DIR=./dbt_project/profiles
